@model List<Model.EF.Menu>
<div class="call">
    <p><span>Need help?</span> call me <span class="number">0974-296-837</span></span></p>
</div>
<div class="account_desc">
    <ul>
        @foreach (var item in Model)
        {
            <li><a href="@item.Link">@item.Text</a></li>
        }
    </ul>
</div>
<div class="clear"></div>
